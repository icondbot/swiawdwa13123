kind = "web"
previewPath = "/"
title = "iCondo Tennis Booker"
version = "1.0.0"
id = "artifacts/icondo-booker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20649

[services.development]
run = "pnpm --filter @workspace/icondo-booker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/icondo-booker", "run", "build" ]
publicDir = "artifacts/icondo-booker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20649"
BASE_PATH = "/"
